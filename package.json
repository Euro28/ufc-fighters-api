{
	"name": "mma-ufc-api",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev",
		"build": "yarn global add pg && yarn install && cd mma-api && git submodule init && git submodule update && yarn install && cd .. && next build",
		"start": "next start",
		"lint": "next lint"
	},
	"dependencies": {
		"dotenv": "^10.0.0",
		"next": "11.1.0",
		"pg": "^8.7.1",
		"pg-hstore": "^2.3.4",
		"react": "17.0.2",
		"react-dom": "17.0.2",
		"sequelize": "^6.6.5"
	},
	"devDependencies": {
		"eslint": "7.32.0",
		"eslint-config-next": "11.1.0"
	}
}
